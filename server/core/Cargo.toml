[package]
name = "rts-core"
version = "0.1.0"
edition = "2021"

[lib]
path = "../src/rules.rs"

[features]
stdb = ["dep:spacetimedb"]

[dependencies]
pathfinding = "4.15.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.151"
spacetimedb = { version = "2.0", optional = true }
